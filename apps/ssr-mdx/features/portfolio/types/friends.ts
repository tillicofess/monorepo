export interface Friend {
	/** Display name of the friend. */
	name: string;
	/** Avatar image URL (absolute or path under /public). */
	avatar: string;
	/** Personal website or social profile URL. */
	url: string;
}
