---
title: OpenCode 使用指南
date: 2026-02-22
---

OpenCode 是一个开源 AI 编程助手，可在终端、桌面或 IDE 中使用。

## 安装

```bash
curl -fsSL https://opencode.ai/install | bash
```

也可以使用包管理器安装：

```bash
npm install -g opencode-ai
```

```bash
pnpm install -g opencode-ai
```

```bash
yarn global add opencode-ai
```

## 快速开始

### 配置

运行 `/connect` 命令选择 AI 提供商并配置 API 密钥。

```bash
/opencode
/connect
```

### 初始化项目

在项目目录中运行以下命令初始化 OpenCode：

```bash
/opencode
/init
```

这会分析项目并创建 `AGENTS.md` 文件，帮助 OpenCode 理解项目结构和编码规范。

## 使用方式

### 提问

使用 `@` 键搜索项目中的文件：

```bash
How is authentication handled in @packages/functions/src/api/index.ts
```

### 添加功能

1. 按 **Tab** 键切换到 Plan 模式
2. 描述你想要的功能
3. 再次按 **Tab** 键切换回 Build 模式执行

### 撤销更改

使用 `/undo` 撤销更改：

```bash
/undo
```

使用 `/redo` 重做：

```bash
/redo
```

### 分享对话

```bash
/share
```

创建当前对话的分享链接。

## 常用命令

| 命令 | 描述 |
|------|------|
| `/init` | 初始化项目分析 |
| `/connect` | 连接 AI 提供商 |
| `/undo` | 撤销更改 |
| `/redo` | 重做更改 |
| `/share` | 分享对话 |

## 配置

参考 [OpenCode 官方文档](https://opencode.ai/docs) 了解：

- [主题配置](https://opencode.ai/docs/themes)
- [快捷键绑定](https://opencode.ai/docs/keybinds)
- [自定义命令](https://opencode.ai/docs/commands)
- [MCP 服务器](https://opencode.ai/docs/mcp-servers)
